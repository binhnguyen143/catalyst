{
	"image": "mcr.microsoft.com/devcontainers/universal:2",
	"features": {
		"ghcr.io/devcontainers/features/postgres-client:1": {}
	},
	"postStartCommand": "sudo service postgresql start && sudo -u postgres psql -c \"ALTER USER postgres WITH PASSWORD 'postgres';\"",
	"remoteEnv": {
		"CATALYST_DATABASE_URL": "postgres://postgres:postgres@localhost:5432/postgres?sslmode=disable"
	}
}